
---

# Angular 前端必學清單 Angular Essential Learning List

---

## 1. Angular 基礎 / Angular Basics

* 元件（Component）與模板（Template）
  Components and Templates
* 模板語法（Template Syntax）
  Template Syntax
* 資料綁定（Data Binding）
  Data Binding

  * 插值（Interpolation）
    Interpolation
  * 屬性綁定（Property Binding）
    Property Binding
  * 事件綁定（Event Binding）
    Event Binding
  * 雙向綁定（Two-way Binding）
    Two-way Binding (`[(ngModel)]`)
* 指令（Directives）
  Directives

  * 結構型指令（\*ngIf, \*ngFor）
    Structural Directives (`*ngIf`, `*ngFor`)
  * 屬性型指令（ngClass, ngStyle）
    Attribute Directives (`ngClass`, `ngStyle`)

---

## 2. 組件溝通 / Component Communication

* Input 和 Output 裝飾器（@Input, @Output）
  `@Input` and `@Output` decorators
* 事件發射器（EventEmitter）
  EventEmitter
* 父子組件傳值
  Parent to Child Communication
* 兄弟組件溝通（Service + Subject）
  Sibling Component Communication (Service + Subject)

---

## 3. 服務與依賴注入 / Services & Dependency Injection

* 建立服務（Service）
  Creating Services
* 依賴注入（Dependency Injection, DI）
  Dependency Injection
* 提供者（Providers）配置
  Providers Configuration

---

## 4. 路由與導航 / Routing & Navigation

* Angular Router 基本用法
  Basic Angular Router Usage
* 路由參數（Route Parameters）
  Route Parameters
* 路由守衛（Route Guards）
  Route Guards (CanActivate, CanDeactivate)
* 子路由（Child Routes）
  Child Routes
* 懶加載模組（Lazy Loading Modules）
  Lazy Loading Modules

---

## 5. 表單處理 / Forms Handling

* 模板驅動表單（Template-driven Forms）
  Template-driven Forms
* 響應式表單（Reactive Forms）
  Reactive Forms
* 表單驗證（Form Validation）
  Form Validation
* 自訂驗證器（Custom Validators）
  Custom Validators

---

## 6. HTTP 與 API 請求 / HTTP & API Requests

* HttpClient 模組
  HttpClient Module
* 發送 GET、POST、PUT、DELETE 請求
  Sending GET, POST, PUT, DELETE requests
* 非同步操作（Observables 與 RxJS）
  Asynchronous Operations (Observables & RxJS)
* 錯誤處理
  Error Handling

---

## 7. RxJS 與響應式編程 / RxJS & Reactive Programming

* Observable 基礎
  Observable Basics
* 常用操作子（Operators）
  Common Operators (`map`, `filter`, `switchMap`, etc.)
* Subject 與 BehaviorSubject
  Subject and BehaviorSubject
* 多重訂閱管理（Subscription Management）
  Subscription Management

---

## 8. 樣式與 UI / Styling & UI

* 內嵌樣式與外部樣式
  Inline and External Styles
* CSS 預處理器（SASS/SCSS）
  CSS Preprocessors (SASS/SCSS)
* Angular Material
  Angular Material UI library
* 自訂主題與響應式設計
  Custom Themes & Responsive Design
* 動畫（Angular Animations）
  Angular Animations

---

## 9. 測試 / Testing

* 單元測試（Jasmine + Karma）
  Unit Testing (Jasmine + Karma)
* 端對端測試（Protractor / Cypress）
  E2E Testing (Protractor / Cypress)

---

## 10. 開發工具與環境 / Dev Tools & Environment

* Angular CLI 使用
  Angular CLI usage
* TypeScript 基礎與進階
  TypeScript Basics & Advanced
* ESLint 與 Prettier
  ESLint and Prettier setup
* Git 與版本控制
  Git and Version Control
* Angular DevTools
  Angular DevTools

---

## 11. 進階主題 / Advanced Topics

* NgModules 模組系統
  NgModules System
* 動態組件載入（Dynamic Component Loading）
  Dynamic Component Loading
* 國際化（i18n）
  Internationalization (i18n)
* PWA 支持
  Progressive Web App (PWA) Support
* Server-Side Rendering（Angular Universal）
  Server-Side Rendering (Angular Universal)

---
